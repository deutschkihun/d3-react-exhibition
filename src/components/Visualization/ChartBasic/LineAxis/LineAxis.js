import React from 'react'
import { Axis } from './Axis'


export const LineAxis = ({
    width,
    height,
    margin
}) => (
    <Axis
        width={width}
        height={height}
        margin={margin}
    />
)

