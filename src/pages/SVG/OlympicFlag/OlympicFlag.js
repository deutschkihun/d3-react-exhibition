import React from 'react'
import {Flag} from './Flag'

export const OlympicFlag = ({
    width,
    height,
    centerX,
    centerY,
    radius,
    strokeWidth
}) => (
    <Flag
        width={width}
        height={height}
        centerX={centerX}
        centerY={centerY}
        radius={radius}
        strokeWidth={strokeWidth}
    />
)


